// 0
// Testing 'it' in expressions and func calls

fn isFifty(int y) -> bool { return y == 50; }

fn main() -> int {

    loop 100 {
        if isFifty($) break;
        int x = $ + 3;
        out x;
    }
    return 0;
}