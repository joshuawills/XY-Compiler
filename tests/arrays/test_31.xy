// 0
// Logging out basic arrs

fn main() -> int {

    arr<int> x = [1, 2, 3];
    out x;
    arr<char> y = ['a', 'b', 'c', 'd'];
    out y;
    arr<bool> z = [true, false, 1 == 1];
    out z;
    arr<str> a = ["hello", "world", "!"];
    out a;
    return 0;

}