fn main() -> int {
    int x = in "Enter your favourite number here :) ";
    if isEven(x) {
        out "This number is even!\n";
    } else {
        out "This number is odd!\n";
    }
    return 0;
}

fn isEven(int x) -> int {
    return (x % 2) == 0;
}