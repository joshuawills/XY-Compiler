define main() -> int {
    // A simple program...
    loop {
        int x = in "What's your favourite number: ";
        if x == 42 {
            out "you have chosen correctly\n";
            break;
        } else {
            out "Try again :(\n";
        }
    }
    return 0;
}