
define getSum(int x, int y, int z) -> int {
    out 3;
    return x + y + z;
}

define main() -> int {
    
    int x = 3;
    int y = 4;
    int z = 5;
    int what = getSum(3, 4, 5);
    out what;
}
