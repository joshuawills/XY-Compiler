// 1
// Basic if 2

define main() -> int {  

    mut int x = 4;

    loop {
        x++;
        if (x == 5) {
            continue;
        }
        out x;
        if (x == 6) { break; }
    }

    x++;
    return x;
}